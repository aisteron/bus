doctype html
<html lang="ru">
head
	meta(charset="UTF-8")
	link(rel='dns-prefetch' href='//fonts.googleapis.com')
	link(href="https://fonts.googleapis.com/css?family=Open+Sans+Condensed:300,700&amp;subset=cyrillic-ext" rel="stylesheet")
	meta(name="viewport" content="width=device-width, initial-scale=1.0")
	link(
		rel="icon" 
		type="image/x-icon"
		href!="<?php echo get_site_url().'/favicon.ico' ?>"
	)
	title
		| <?php wp_title(); ?>
	| <?php wp_head()?>
<body>
<div class='wrap'>
	.logo-line
		.logo
			a(href="/") 
				i.sprite.sprite-logo
		.mail
			i.sprite.sprite-mail
			span busminsk@bk.ru
		.phone
			i.sprite.sprite-phone
			span 
				a(href="tel:+375296387700") +375 29 638 77 00
		.vw
			span.wrap
				i.sprite.sprite-viber
				span мы есть в viber
			span.wrap
				i.sprite.sprite-wa
				span и whatsapp
		.grid-icon
			i.sprite.sprite-grid
	.topnav
		| <?php wp_nav_menu(array('menu' => 'header-menu','container' => ''))
		|?>
	style(type="text/css").
		.sprite {background-image:url(/wp-content/themes/bus/src/img/sprite.png)}
		